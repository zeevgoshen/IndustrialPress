/** Phase 6: SignalR live updates for all 20 sensors — no polling */
export default function LiveDashboard() {
  return (
    <section>
      <h1>Live Dashboard</h1>
      <p>Phase 0 placeholder — connect SignalR in Phase 6.</p>
    </section>
  );
}
