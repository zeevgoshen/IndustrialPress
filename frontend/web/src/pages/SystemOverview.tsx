/** Phase 6: REST health / system status */
export default function SystemOverview() {
  return (
    <section>
      <h1>System Overview</h1>
      <p>Phase 0 placeholder — call GET /api/health via REST.</p>
    </section>
  );
}
